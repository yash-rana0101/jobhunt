import { PageShell } from "./components/page-shell";

export default function HomePage() {
  return (
    <PageShell
      eyebrow="Command Center"
      title="Autonomous job hunting workspace"
      description="Placeholder dashboard for discovering jobs, matching roles, finding contacts, managing outreach, and tracking applications."
    />
  );
}
