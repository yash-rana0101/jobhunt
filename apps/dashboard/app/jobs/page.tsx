import { PageShell } from "../components/page-shell";

export default function JobsPage() {
  return (
    <PageShell
      eyebrow="Jobs"
      title="Opportunity discovery"
      description="Placeholder for matched roles, target companies, job scores, and saved searches."
    />
  );
}
